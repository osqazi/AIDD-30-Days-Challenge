# Contracts for Simple Expression Calculator

This is a Command-Line Interface (CLI) application and does not expose any external APIs or formal contracts (e.g., OpenAPI). Its primary interface is through standard input/output.
